// Remove this file as it's causing confusion with the actual content files
// We'll use the content from the markdown files directly
import type { Post } from "./types"

export const allPosts: Post[] = []

